from aiogram.fsm.state import StatesGroup, State


class PrescriptionFSM(StatesGroup):
    # ==========================================
    # 🏥 Общие / Начало (General)
    # ==========================================

    # Выбор района и маршрута (если используются)
    picking_district = State()
    picking_road = State()

    # Выбор ЛПУ/Аптеки
    # В коде add.py мы используем picking_lpu, поэтому называем так
    picking_lpu = State()

    # Для совместимости (если где-то осталось старое название)
    choose_lpu = picking_lpu
    choose_apothecary = State()

    # ==========================================
    # 👨‍⚕️ Ветка: Визит к Врачу (Doctor Flow)
    # ==========================================

    # Выбор врача
    # В коде add.py мы используем picking_doc
    picking_doc = State()

    # Для совместимости
    choose_doctor = picking_doc

    contract_terms = State()  # Ввод условий
    doctor_comments = State()  # Комментарий

    # ==========================================
    # 💊 Ветка: Лекарства и Аптека
    # ==========================================
    choose_meds = State()  # Мульти-выбор препаратов (Checkboxes)
    waiting_for_req_qty = State()  # Ввод заявки
    waiting_for_rem_qty = State()  # Ввод остатка

    waiting_for_quantity = State()
    waiting_for_remaining = State()
    pharmacy_comments = State()

    # ==========================================
    # ✅ Финал (Finalization)
    # ==========================================
    confirmation = State()  # Экран проверки